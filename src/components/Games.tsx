export const Games = () => {
  return (
    <div style={{ paddingTop: "50px" }}>
      Games currently are not supported, but I don{"'"}t really feel like
      changing the name of the website, so this page still exists. But I do
      fulfill all requirements using the messages page
    </div>
  );
};
